name: Go

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ '**' ]

jobs:

  build:
    runs-on: golang:alpine3.14

    steps:
    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
